services:
  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  app:
    build: .
    ports:
      - "3001:3000"
    environment:
      - MONGO_URI=mongodb://mongodb:27017/blog_dev
    depends_on:
      - mongodb

volumes:
  mongo_data:
